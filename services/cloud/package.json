{
  "name": "@sb/cloud",
  "version": "1.0.0",
  "private": true,
  "description": "SB Launcher Cloudflare Worker — player registry + update manifest",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250719.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.26.0"
  }
}
